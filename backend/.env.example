# MySQL no EasyPanel
#
# Dentro do EasyPanel (backend deployado):
# DATABASE_URL="mysql://mysql:SENHA@auto_eventhub:3306/eventhub-db"
#
# Do seu PC (MySQL exposto na aba "Expor"):
# DATABASE_URL="mysql://mysql:SENHA@HOST_PUBLICO:3306/eventhub-db"

DATABASE_URL=""

JWT_SECRET="troque-por-um-segredo-forte"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_SECRET="troque-por-outro-segredo-forte"
JWT_REFRESH_EXPIRES_IN="7d"
PORT=3001
# Origens permitidas — sem barra no final (ex.: https://eventhub-drab.vercel.app)
CORS_ORIGIN="http://127.0.0.1:3000,http://localhost:3000"
# URL pública do frontend (Vercel ou EasyPanel)
FRONTEND_URL="https://eventhub-drab.vercel.app"
FLYER_MAX_MB=15

# EasyPanel: monte um volume persistente em /app/uploads no serviço do backend.
# Sem volume, os flyers somem a cada redeploy do container.

# Chave para criptografar credenciais de gateway (Inter, etc.) — mínimo 32 caracteres
PAYMENTS_ENCRYPTION_KEY="troque-por-uma-chave-forte-com-32-caracteres-ou-mais"

# true = checkout Pix usa mock (dev local sem certificado Inter)
MOCK_PIX="true"
# true = checkout Boleto usa mock. Se omitido, boleto usa Inter quando configurado.
MOCK_BOLETO="false"
# URL pública do backend (EasyPanel) — usada nos webhooks InfinitePay
BACKEND_PUBLIC_URL="https://auto-eventhub-backend.4tnf3f.easypanel.host"

# true = checkout InfinitePay mockado (dev local)
MOCK_INFINITYPAY="false"
